# Frontend Production Environment Configuration Template
#
# SECURITY WARNING: Never commit actual .env.production files to git!
# This is a template file only. Copy this to .env.production and fill in
# with your actual production values.
#
# Usage:
#   1. Copy this file: cp .env.production.example .env.production
#   2. Update the values with your production configuration
#   3. NEVER commit the .env.production file
#
# For deployment, these values should be injected at build time via:
#   - CI/CD environment variables
#   - Docker secrets
#   - Kubernetes ConfigMaps/Secrets
#   - Cloud provider secret managers (AWS Secrets Manager, GCP Secret Manager)

# API Configuration
# The base URL for the backend API (without trailing slash)
# Example production values:
#   - https://api.yourapp.com/api/v1
#   - https://yourapp.com/api/v1
VITE_API_BASE_URL=https://your-production-api-domain.com/api/v1

# Environment
# Must be "production" for production builds
VITE_ENV=production

# Optional: OAuth Configuration (if using OAuth providers)
# VITE_GOOGLE_CLIENT_ID=your-google-client-id
# VITE_GITHUB_CLIENT_ID=your-github-client-id

# Optional: Feature Flags
# VITE_ENABLE_ANALYTICS=true
# VITE_ENABLE_ERROR_TRACKING=true

# Optional: Application Metadata
# VITE_APP_VERSION=1.0.0
# VITE_BUILD_DATE=2025-12-06
